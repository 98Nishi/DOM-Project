const boxElement = document.getElementById("box");

// // add a mouseout event listener to the element
boxElement.addEventListener("mouseout", function (event) {
  event.target.style.color = "Black";

});


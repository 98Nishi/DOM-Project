const boxElement = document.getElementById("box");

// add a mouseover event listener to the element

boxElement.addEventListener("mouseover", function () {
  alert(
    "A mouse-over (also known as a mouse hover) is an action that happens when the user stops or stays close to the on-screen mouse pointer above a particular element on a computer screen.(mouse over event is working properly)"
  );
});